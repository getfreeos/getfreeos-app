#!/bin/bash
#set -e
echo "#################################"
echo " Start!"
echo "#################################"
sudo pacman -Sy archlinux-keyring && pacman -Su
sudo pacman-key --init
sudo pacman-key --populate
echo "#################################"
echo " Done!"
echo "#################################"
